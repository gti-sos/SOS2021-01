name: CI

on: 
  push:
    branches: [ main ]
  
jobs:
  integrationTests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm start &
      - run: npx newman tests/NatalityStatsAPI_postman.json && npx newman tests/LifeStatsAPI_postman.json